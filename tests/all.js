const assert = require('assert');
const { isValidVerseInOriginal } = require('../build/main.bundle.js');

describe('Versification', () => {

  describe('isValidVerseInOriginal', () => {

    it('should work', () => {
      var isValid = true
      assert.equal(isValid, true)
    })

  })

})